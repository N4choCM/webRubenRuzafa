import { readFileSync, writeFileSync, mkdirSync } from 'node:fs';
import { dirname, join } from 'node:path';
import { fileURLToPath } from 'node:url';

const __dirname = dirname(fileURLToPath(import.meta.url));
const root = join(__dirname, '..');

const es = JSON.parse(readFileSync(join(root, 'src/locales/es.json'), 'utf-8'));
const en = JSON.parse(readFileSync(join(root, 'src/locales/en.json'), 'utf-8'));

const knowledge = {
  profile: {
    name: 'Rubén Ruzafa Cueto',
    role: 'Personal trainer (former professional triathlete and mountain biker)',
    location: 'Rincón de la Victoria, Málaga, Spain',
    credentials: ['CCAFD graduate', 'Level 2 triathlon coach'],
    titles: [
      '3x XTERRA World Champion (2008, 2013, 2014)',
      '4x ITU Cross Triathlon World Champion (2014, 2015, 2016, 2018)',
      'Spanish MTB XCO Champion (2005, 2006, 2008)',
    ],
    status: 'Retired from professional competition, now focused on coaching',
  },
  services: {
    es: es.services,
    en: en.services,
  },
  about: {
    es: es.about,
    en: en.about,
  },
  career: {
    es: es.career,
    en: en.career,
  },
  contact: {
    whatsapp: 'Available via WhatsApp on the website',
    social: {
      instagram: 'https://www.instagram.com/rubenruzafa/',
      facebook: 'https://www.facebook.com/ruben.ruzafa',
      twitter: 'https://twitter.com/rubenruzafa',
    },
    form: 'Contact form available on the website (EmailJS)',
  },
  pricing: 'Pricing is not listed on the website. Direct users to contact Rubén for details.',
};

const output = `// Auto-generated by scripts/generate-knowledge.js — do not edit manually
export const KNOWLEDGE_JSON = ${JSON.stringify(JSON.stringify(knowledge))};
`;

const outDir = join(root, 'src/generated');
mkdirSync(outDir, { recursive: true });
writeFileSync(join(outDir, 'knowledge.js'), output);
console.log('Generated src/generated/knowledge.js');
